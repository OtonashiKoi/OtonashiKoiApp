# 工程 TODO

> 狀態：執行清單。最後更新：2026-08-14。
>
> 這份清單只安排工作順序，不是遊戲現況的證明。執行事實仍以程式碼與目前 MongoDB 為準；完成項目必須附驗證結果後才能勾選。

## 現在做

- [ ] **盤點並拆分目前工作區變更**
  - 將已修改、刪除與未追蹤檔案逐一歸入功能批次，避免不同功能混在同一個 commit。
  - 暫定批次：管理後台／Studio、戰鬥與活動、直播與帳號綁定、玩家與世界王、SPA 部署產物、文件。
  - `_tmp_fat.js`、舊 SPA hash、備份 JSON 等疑似暫存或舊產物只做分類；刪除前需使用者核准。
  - 完成條件：每個變更都有歸屬、測試方式與是否保留的決定，且未改動內容。

- [ ] **逐批驗證並提交現有功能**
  - 每一批先跑對應測試，再跑 `npm run test:all`。
  - 玩家可見行為、功能開關、路由、戰鬥或活動規則有變更時，同步現行文件並跑 `npm run check:docs`。
  - 完成條件：每批獨立 commit、測試全綠、推送後本地與遠端同步。

## 接著做

- [ ] **修正 `streamAccountBindings` 正規化重複資料**（正式 DB 變更，需核准）
  - 已知同一 Twitch ID 同時存在純數字與 `tw-` 前綴形式；先備份相關記錄，再決定保留與合併規則。
  - 完成條件：正規化後沒有重複綁定，既有訂閱者／會員快照沒有遺失，綁定回歸測試通過。

- [ ] **重建直播帳號綁定唯一索引**（正式 DB 結構變更，需核准）
  - 必須在重複資料修正後執行；索引規格需和 runtime 一致。
  - 完成條件：啟動不再出現索引衝突警告，重複綁定會被資料庫拒絕。

- [ ] **逐步降低大型檔案行數債務**
  - 保留目前 ratchet：新檔硬上限 400 行，既有大檔不可無限制成長。
  - 每次只拆一個高變動檔，先補回歸測試，再抽出單一職責模組。
  - 完成條件：行數基線下降、`npm run check:lines` 與 `npm run test:all` 通過。

- [ ] **建立 golden snapshot 更新規則**
  - 只有戰鬥結果的預期行為刻意改變時才重新生成；一般失敗先視為回歸問題。
  - 完成條件：更新 commit 同時包含原因、fixture 變更與差異檢查，重跑結果完全一致。

## 之後做

- [ ] **爬塔擴大測試前補齊持久化與結算保護**
  - 房間／場次需能處理服務重啟；獎勵結算需以 `runId` 或等價鍵保證冪等。
  - 先維持小規模測試，不直接擴大公開範圍。
  - 完成條件：重啟復原、重複結算與 10 樓流程測試通過後，再決定是否擴大。

## 每批固定品質閘門

1. 變更前備份受影響檔案；DB 操作另做資料備份。
2. 跑功能對應測試。
3. 跑 `npm run test:all`，目前標準為 9/9 通過。
4. 跑 `git diff --check`，核對 staged diff 不混入其他工作。
5. 必要時更新現行文件並跑 `npm run check:docs`。
6. commit、push，確認本地與遠端 ahead/behind 為 0/0。

## 已完成

- [x] 將行數限制改為「新檔硬限制＋既有大檔 ratchet」。
- [x] 將戰鬥 golden test 改為固定 fixture，不依賴正式玩家或即時 MongoDB 狀態。
- [x] 建立 `npm run test:all`，9 個核心套件可一次驗證並自然結束。
- [x] 品質基線已於 commit `4d9c94dd` 推送至 `agent/game-fixes-20260810`。
